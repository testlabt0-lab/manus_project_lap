# MediCare Pro — النسخة الوظيفية الموسعة

## الغرض ونطاق الاستخدام

تجسّد هذه النسخة نموذجاً وظيفياً عربياً متجاوباً لإدارة الزيارات المنزلية ضمن عيادة، بدءاً من إنشاء الحجز ومروراً بالتكليف والتشغيل وانتهاءً بالمخرج السريري والفاتورة. وقد صُمّمت جميع المسارات كي تستخدم **بيانات تجريبية آمنة فقط**؛ فلا تُدخل إليها أسماء مرضى حقيقية أو معلومات صحية فعلية أو وسائل دفع أو مراجع دفع صادرة من مزود حقيقي.

> لا تمثل النسخة نظاماً سريرياً منتجاً، ولا تُعد بديلاً عن التقييم الطبي أو نظام فوترة أو بوابة دفع حقيقية.

## المسارات الوظيفية

| الوظيفة | الإجراء أو المسار | ضابط الحماية المطبق |
|---|---|---|
| إنشاء حجز | `visits.create` | يتطلب جلسة مصادقة، ويربط الخادم `patientId` بالمستخدم الحالي. |
| عرض زيارات المريض وتفاصيلها | `visits.listMine` و`visits.getMine` | لا يعاد المورد إلا إذا كان المريض مالك الزيارة. |
| قائمة تشغيل المدير | `visits.listOperations` | مقصورة على المدير وتخضع لعضوية عيادة نشطة. |
| قائمة أعضاء الفريق المؤهلين | `memberships.listStaffForOperations` | لا تعيد إلا عضويات `CLINICIAN` أو `NURSE` النشطة ضمن عيادات المدير. |
| إنشاء عضو تجريبي آمن | `memberships.ensureDemoClinician` | إجراء إداري ينشئ عضواً تجريبياً غير شخصي في نطاق عيادة المدير عند الحاجة للعرض. |
| تعيين زيارة لعضو فعلي | `visits.assign` | يقبل `assigneeUserId` ويتحقق من أن العضو نشط ومؤهل وفي عيادة الزيارة نفسها. |
| قائمة مهام العضو | `visits.listAssignedToMe` | لا تعيد إلا الزيارات التي يطابق فيها `assigneeUserId` المستخدم الحالي وعضويته النشطة. |
| انتقال الحالة | `visits.transition` | يمنع القفزات خارج آلة حالات الزيارة ويكتب سجلاً للحالة. |
| إقفال التقرير النهائي | `outputs.finalizeReport` | يتطلب ممارساً نشطاً مكلّفاً بالزيارة المكتملة، ويمنع إنشاء تقرير ثانٍ للزيارة. |
| تقرير المريض النهائي | `outputs.reportMine` | لا يعرض إلا تقرير زيارة مكتملة ومملوكة للمريض الحالي. |
| الفاتورة والسداد التجريبي | `outputs.invoiceMine` و`outputs.recordDemoPayment` | تقصر الفاتورة على المريض المالك، وتسمح بالتسجيل التجريبي مرة واحدة فقط لحالة `DUE`. |

## تجربة الواجهة

تتيح لوحة المدير اختيار عضو فعلي من قائمة الطاقم الخادمية قبل تفعيل زر التعيين؛ ولذلك لا يمكن استخدام اسم نصي حر كبديل لمعرف العضو. عندما لا يتوافر عضو مؤهل في بيئة العرض، يظهر خيار صريح لإنشاء **ممارس تجريبي آمن**. هذا الحساب مخصص للعرض فقط ولا يحمل بريداً أو ملفاً صحياً أو هوية شخص حقيقي.

تعرض مساحة الفريق الطبي المهام المسندة إلى الحساب الحالي فقط. وتظهر وصلة تحرير التقرير النهائي للممارس المكلّف عندما تكون الزيارة مكتملة، مع تمرير معرف الزيارة إلى المسار `/team/report/:id`. يعيد الخادم التحقق الكامل عند الحفظ، ولذلك لا يكفي الوصول إلى الرابط أو تعديل معرف الزيارة في المتصفح لتجاوز صلاحية التكليف.

تظهر الفاتورة للمريض بعد اكتمال الزيارة فقط، بينما لا يُتاح زر **السداد التجريبي** إلا إذا كانت حالة الفاتورة `DUE`. يسجل الإجراء مرجعاً اصطناعياً من صيغة `DEMO-…` ويحوّل حالة الفاتورة إلى `PAID` دون نقل أموال أو جمع أي بيانات دفع.

## نموذج البيانات المستخدم

| الجدول | مسؤوليته في التدفق |
|---|---|
| `visits` | الطلب والموعد والعيادة وحالة الزيارة ومالكها. |
| `clinic_memberships` | يربط المستخدم بالعيادة والدور التشغيلي وحالة العضوية. |
| `visit_assignments` | يربط الزيارة بالمدير المكلف وبـ`assigneeUserId` الفعلي. |
| `visit_status_history` | يسجل انتقالات الحالة لأغراض التدقيق. |
| `medical_reports` | يحفظ التقرير النهائي الواحد ومرجع الممارس الذي أقفله. |
| `invoices` | يصدر عند اكتمال الزيارة ويحمل حالة الاستحقاق أو السداد. |
| `payments` | يسجل نتيجة الدفع التجريبي فقط مع مرجع اصطناعي. |

## الاختبارات والتحقق

يجري التحقق البرمجي بالأمر `pnpm check && pnpm test`. تغطي الاختبارات الحالية إنشاء الحجز، التفويض الإداري للتعيين، تمرير معرف عضو الفريق الفعلي، قائمة مهام العضو المقيدة بهويته، منع انتقالات الحالة غير المسموحة، ملكية التقرير والفاتورة، تسجيل السداد التجريبي، وإنشاء الممارس التجريبي عبر سياق المدير. كما تمت مراجعة واجهات لوحة المدير ومساحة الفريق ومحرر التقرير على شاشات مكتبية ومحمولة بعد معالجة تجاوز العرض في RTL.

## الحدود والضوابط المتبقية

تطبّق هذه النسخة مصادقة خادمية ونطاقاً للعيادة وقيود ملكية وإجراءات تفويض، لكنها **لا تطبق Row-Level Security فعلية** لأن قاعدة البيانات الحالية MySQL/TiDB؛ لذا لا ينبغي وصفها كتنفيذ RLS إنتاجي. كذلك لا تحتوي على تكامل دفع حقيقي أو تشفير مخصص للمحتوى السريري أو إدارة وصول مؤسسية كاملة أو سجلات امتثال قانوني.

قبل استخدام النظام خارج العرض الأكاديمي، يلزم استبدال العضو التجريبي بإدارة حسابات حقيقية ومراجعة نموذج الصلاحيات، وتطبيق تشفير مناسب وإدارة مفاتيح وسجل تدقيق غير قابل للعبث وسياسات الاحتفاظ بالبيانات، فضلاً عن مراجعة قانونية وأمنية مستقلة.
