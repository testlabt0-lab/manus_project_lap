# MediCare Pro — النسخة الوظيفية الأولى

تضيف هذه النسخة طبقة خادمية لرحلة الزيارة فوق واجهات RTL الموجودة. تظل البيانات المستخدمة في العرض والاختبار **تجريبية وآمنة**، ولا تتضمن سجلات مرضى حقيقية أو تقارير طبية فعلية أو بيانات دفع.

| الوظيفة | المسار الخادمي | الحماية الحالية |
|---|---|---|
| إنشاء حجز | `visits.create` | جلسة مصادقة مطلوبة؛ يربط الخادم `patientId` بالمستخدم الحالي. |
| قائمة زيارات المريض | `visits.listMine` | لا تعيد إلا الزيارات التي يملكها المستخدم الحالي. |
| تفاصيل زيارة فعلية | `visits.getMine` | يتطلب تسجيل الدخول ومعرفاً رقمياً؛ يعرض حالة آمنة عند عدم الملكية أو عدم العثور على المورد. |
| قائمة تشغيل المدير | `visits.listOperations` | إجراء إداري محمي. |
| تعيين الفريق | `visits.assign` | إجراء إداري؛ يقبل فقط زيارة في حالة `REQUESTED`. |
| انتقال الحالة | `visits.transition` | يتحقق من State Machine الخادمي ويرفض القفز بين الحالات. |

## الجداول المضافة

تستخدم النسخة جداول `visits` و`visit_assignments` و`visit_status_history`. تشمل الفهارس الأولية قائمة الزيارات حسب المريض والموعد، وقائمة التشغيل حسب الحالة والموعد، وسجل تغييرات الزيارة حسب الزيارة والزمن.

## ما تم اختباره

تغطي اختبارات Vitest الحالية المصادقة، وسياسة انتقال الحالة، ومنع الوصول غير الموثق، وإنشاء الحجز، وتعيين الفريق، ورفض انتقال حالة غير مسموح، إضافة إلى بيانات العرض التجريبية. يشغّل الفحص عبر `pnpm check && pnpm test`.

## حدود الإصدار

هذه ليست بيئة إنتاج سريرية. لا تزال العضويات الدقيقة للعيادات، وتفويض كل عضو من الفريق الطبي، وتشفير التقرير الطبي، والتكامل مع مزود دفع حقيقي، وسياسات RLS ذات المحرك المناسب، مراحل لاحقة. لا ينبغي إدخال بيانات صحية حقيقية قبل إكمال تلك الضوابط ومراجعة الأمن والامتثال.
