# MediCare Pro — النسخة الوظيفية الموسعة

## الغرض ونطاق الاستخدام

تجسّد هذه النسخة نموذجاً وظيفياً عربياً متجاوباً لإدارة الزيارات المنزلية ضمن عيادة، بدءاً من إنشاء الحجز ومروراً بالتكليف والتشغيل وانتهاءً بالمخرج السريري والفاتورة. وقد صُمّمت جميع المسارات كي تستخدم **بيانات تجريبية آمنة فقط**؛ فلا تُدخل إليها أسماء مرضى حقيقية أو معلومات صحية فعلية أو وسائل دفع أو مراجع دفع صادرة من مزود حقيقي.

> لا تمثل النسخة نظاماً سريرياً منتجاً، ولا تُعد بديلاً عن التقييم الطبي أو نظام فوترة أو بوابة دفع حقيقية.

## المسارات الوظيفية

| الوظيفة | الإجراء أو المسار | ضابط الحماية المطبق |
|---|---|---|
| إنشاء حجز | `visits.create` | يتطلب جلسة مصادقة، ويربط الخادم `patientId` بالمستخدم الحالي. |
| عرض زيارات المريض وتفاصيلها | `visits.listMine` و`visits.getMine` | لا يعاد المورد إلا إذا كان المريض مالك الزيارة. |
| قائمة تشغيل المدير | `visits.listOperations` | مقصورة على المدير وتخضع لعضوية عيادة نشطة. |
| قائمة أعضاء الفريق المؤهلين | `memberships.listStaffForOperations` | لا تعيد إلا عضويات `CLINICIAN` أو `NURSE` النشطة ضمن عيادات المدير. |
| سجل عضويات الفريق للمدير | `memberships.listManagedStaff` | يعرض عضويات الممارس والتمريض، بما فيها المعلّقة، داخل عيادات المدير فقط. |
| تفعيل العضوية أو تعليقها | `memberships.setStaffStatus` | يرفض الخادم تعديل المدير لعضوية خارج عيادته أو عضوية دور إداري. |
| سجل التدقيق التشغيلي | `audit.listOperations` | لا يعيد إلا آخر 50 حدثاً في عيادات المدير النشطة، ويدعم ترشيح العيادة ونوع الحدث وآخر 24 ساعة أو 7 أو 30 يوماً، وبحثاً نصياً محدوداً في الملخص، ويمنع الوصول لغير المدير. |
| تصدير سجل التدقيق | `audit.exportCsv` | يعيد ملف CSV للنتائج المصفاة نفسها ضمن نطاق المدير والعيادة المختارة إن وجدت، ويقتصر على حقول التدقيق التشغيلية الآمنة. |
| التنبيهات التشغيلية | `alerts.listOverdue` | يعيد الزيارات النشطة المتأخرة ضمن عيادات المدير فقط، وفق مهلة من 0 إلى 1440 دقيقة. |
| مركز إشعارات المدير | `notifications.listMine` و`notifications.acknowledge` و`notifications.acknowledgeAll` | ينشئ ويعرض إشعارات داخل التطبيق للزيارات المتأخرة، ويقصر التأكيد الفردي أو الجماعي على المدير وعياداته، ويسجل أول تأكيد لكل إشعار كحدث تدقيق ضمن العيادة. |
| إنشاء عضو تجريبي آمن | `memberships.ensureDemoClinician` | إجراء إداري ينشئ عضواً تجريبياً غير شخصي في نطاق عيادة المدير عند الحاجة للعرض. |
| تعيين زيارة لعضو فعلي | `visits.assign` | يقبل `assigneeUserId` ويتحقق من أن العضو نشط ومؤهل وفي عيادة الزيارة نفسها. |
| قائمة مهام العضو | `visits.listAssignedToMe` | لا تعيد إلا الزيارات التي يطابق فيها `assigneeUserId` المستخدم الحالي وعضويته النشطة. |
| انتقال الحالة | `visits.transition` | يمنع القفزات خارج آلة حالات الزيارة ويكتب سجلاً للحالة. |
| إقفال التقرير النهائي | `outputs.finalizeReport` | يتطلب ممارساً نشطاً مكلّفاً بالزيارة المكتملة، ويمنع إنشاء تقرير ثانٍ للزيارة. |
| تقرير المريض النهائي | `outputs.reportMine` | لا يعرض إلا تقرير زيارة مكتملة ومملوكة للمريض الحالي. |
| الفاتورة والسداد التجريبي | `outputs.invoiceMine` و`outputs.recordDemoPayment` | تقصر الفاتورة على المريض المالك، وتسمح بالتسجيل التجريبي مرة واحدة فقط لحالة `DUE`. |

## تجربة الواجهة

تتيح لوحة المدير اختيار عضو فعلي من قائمة الطاقم الخادمية قبل تفعيل زر التعيين؛ ولذلك لا يمكن استخدام اسم نصي حر كبديل لمعرف العضو. عندما لا يتوافر عضو مؤهل في بيئة العرض، يظهر خيار صريح لإنشاء **ممارس تجريبي آمن**. هذا الحساب مخصص للعرض فقط ولا يحمل بريداً أو ملفاً صحياً أو هوية شخص حقيقي.

تضيف لوحة التشغيل كذلك جدولاً لإدارة عضويات الممارسين والتمريض. يستطيع المدير مشاهدة حالة كل عضوية ضمن عيادته وتعليقها أو إعادة تفعيلها، بينما تبقى العضوية المعلقة خارج قائمة التكليف وقائمة مهام العضو. لا تسمح الواجهة ولا الخادم بتحويل هذه العملية إلى إدارة أدوار المدير أو عضويات العيادات الأخرى.

تظهر في اللوحة أيضاً قائمة تدقيق تشغيلية منفصلة، تسجل التكليف وتغييرات حالة الزيارة وتفعيل العضوية أو تعليقها. يحمل كل حدث نوعاً واسم العيادة وملخصاً تشغيلياً ومعرّف المورد والمستخدم المنفذ والطابع الزمني، من دون نسخ محتوى التقرير السريري أو بيانات وسائل الدفع إلى السجل. يستخرج الخادم اسم العيادة من عضوية المدير النشطة التي حددت نطاق الاستعلام، ولذلك لا يعرض اسم عيادة خارج صلاحية المدير. لا يوفر التطبيق إجراءً لتعديل هذه الأحداث أو حذفها؛ ومع ذلك لا ينبغي اعتبار ذلك بديلاً عن سجل امتثال غير قابل للعبث في بيئة إنتاجية.

يمكن للمدير فتح نافذة تفاصيل آمنة لأي حدث ظاهر في سجل التدقيق. يعيد الخادم الحدث فقط إذا كان داخل عيادة يملك فيها المدير عضوية `MANAGER` نشطة، ويقتصر العرض على نوع الحدث والملخص التشغيلي والمنفذ والوقت ومعرفات العيادة والمورد. لا تعرض النافذة محتوى تقرير سريري أو بيانات دفع أو بيانات تعريف مريض إضافية، وتظهر حالة خطأ عندما يصبح الحدث خارج نطاق الصلاحية أو غير متاح.

يمكن للمدير تضييق القائمة عبر نوع الحدث والفترة الزمنية الجاهزة أو نطاق تاريخ مخصص من تاريخ بداية إلى تاريخ نهاية. يعامل النطاق المخصص اليومين كمدى شامل، من بداية يوم البداية إلى نهاية يوم النهاية بالتوقيت المحلي للواجهة، ويتقدم على الفترة الجاهزة عند تطبيقه. تتحقق الواجهة من إدخال التاريخين ومن أن البداية لا تأتي بعد النهاية قبل إرسال الطلب، ويعيد عقد الخادم رفضاً دفاعياً للنطاق المقلوب. تبقى معايير التصفية جزءاً من الاستعلام الخادمي نفسه، لا مجرد إخفاء بصري للنتائج، ويطبق الخادم شرط العيادة النشطة دائماً قبل أي مرشح إضافي.

يدعم السجل كذلك بحثاً نصياً قصيراً في الملخص التشغيلي؛ ويبدأ عند إدخال حرفين ويقبل حتى 80 حرفاً. وبما أن مرجع الزيارة يظهر في ملخص أحداث التكليف والحالة، يمكن العثور عليه بالبحث من دون توسيع السجل ليتضمن حقولاً إضافية أو محتوى سريرياً. يبقى حد النتيجة 50 حدثاً ثابتاً للحفاظ على قابلية عرض لوحة التشغيل.

يتيح زر **تنزيل CSV** للمدير الحصول على النتائج المطابقة للمرشحات والبحث الحاليين، بما فيها العيادة ونطاق التاريخ المخصص عند تطبيقه. يتضمن الملف التاريخ بصيغة UTC واسم العيادة ونوع الحدث والملخص واسم المنفذ ونوع المورد ومعرفه فقط. يعالج الخادم الأسطر وعلامات الاقتباس، ويحيّد بدايات صيغ الجداول الحسابية في الخلايا، ولا يضمّن التقرير السريري أو بيانات الدفع أو تفاصيل هوية المريض.

يوفر مركز الإشعارات لقطات تحليلية تاريخية يلتقطها المدير يدوياً للعيادة المختارة وفترة 7 أو 30 أو 90 يوماً. تحفظ اللقطة إجمالي الإشعارات والمؤكدة وغير المؤكدة ونسبة التأكيد ومتوسط الاستجابة ووقت الالتقاط فقط، ولا تحفظ عنوان الإشعار أو رسالته أو أي بيانات مريض. لا توجد مهمة دورية أو عملية خارجية في هذه النسخة؛ ويمنع الخادم التقاط اللقطة أو قراءتها من عيادة لا يملك فيها المدير عضوية `MANAGER` نشطة.

تدعم لوحة توافر الفريق تسجيل فترات عمل محددة زمنياً لممارس أو عضو تمريض نشط داخل العيادة. يتحقق الخادم من عضوية المدير النشطة ومن عضوية عضو الفريق النشطة داخل العيادة نفسها ومن أن نهاية الفترة تأتي بعد بدايتها، ثم يحفظ الفترة أو يسمح بإلغائها للمدير المخول فقط. لا تمثل الفترة تكليف زيارة، ولا تنفذ هذه النسخة كشف تعارضات تلقائياً أو جدولة خارجية أو حفظ أي بيانات مريض.

تضيف لوحة التشغيل بطاقة للتنبيهات المتأخرة. تعتمد القاعدة على الزيارات النشطة فقط، أي الحالات غير المكتملة وغير الملغاة، التي تجاوز موعدها مهلة يختارها المدير في الواجهة. لا ترسل هذه النسخة إشعاراً خارج التطبيق ولا تنفذ مهمة دورية؛ بل تحسب القائمة عند فتح لوحة المدير أو تغيير المهلة، وهو حد مقصود للنموذج التوضيحي.

يتوافر مركز إشعارات مستقل من زر الجرس في شريط التطبيق. عند فتحه، ينشئ الخادم عند الحاجة إشعاراً داخلياً واحداً للمدير عن كل زيارة متأخرة ضمن عيادته، ثم يعرض آخر 30 إشعاراً. يمكن للمدير تأكيد إشعار واحد أو كل الإشعارات غير المؤكدة في نطاق عياداته؛ ويعيد الإجراء الجماعي عدد الإشعارات التي عالجها. عند أول تأكيد للاطلاع يحدّث الخادم وقت التأكيد ويكتب حدث `NOTIFICATION_ACKNOWLEDGED` واحداً في `audit_events` لكل إشعار، مرتبطاً بمعرف الإشعار والعيادة والمدير؛ ولا ينشئ حدثاً ثانياً عند إعادة المحاولة. يخزن النظام ملخصاً تشغيلياً محدوداً ووقت التأكيد فقط، ولا يخزن محتوى سريرياً أو بيانات دفع، ولا يرسل بريدًا أو رسالة نصية أو طلباً إلى خدمة خارجية.

يدعم مركز الإشعارات مرشحاً محلياً للحالة: كل الإشعارات، وغير المؤكدة، والمؤكدة. يطبق المرشح على القائمة التي أعادها الخادم بعد تقييدها مسبقاً بعضويات المدير النشطة، لذلك لا يوسع نطاق البيانات. تظهر الواجهة عداد النتائج وتعرض حالة فراغ خاصة إذا لم تطابق الإشعارات مرشح الحالة المختار.

يمكن للمدير كذلك ترتيب النتائج المعروضة حسب الإشعارات غير المؤكدة أولاً، أو الأحدث أولاً، أو الأقدم أولاً. ينفذ الترتيب محلياً على نفس القائمة المقيدة خادمياً، ولا يغير السجل المخزن أو صلاحيات الوصول.

تدعم مؤشرات الاستجابة فترة تقرير يختارها المدير من 7 أو 30 أو 90 يوماً. يتحقق عقد الإجراء من القيم المسموحة فقط، ثم يطبق الخادم حد التاريخ على الإشعارات التي استرجعها داخل نطاق عضويات المدير النشطة قبل احتساب العدد ونسبة التأكيد ومتوسط الاستجابة.

تعرض الواجهة مقارنة زمنية للفترة المختارة مع الفترة السابقة المناظرة لها بالطول نفسه. ففي اختيار 7 أو 30 أو 90 يوماً، تمثل الفترة الحالية المجال من وقت الطلب ناقص عدد الأيام إلى وقت الطلب، وتمثل الفترة السابقة المجال الذي يسبقها مباشرة وبالطول نفسه. تعرض المقارنة نسبة التأكيد وعدد الإشعارات غير المؤكدة في كل نطاق، إلى جانب فرق نسبة التأكيد بالنقاط المئوية وفرق عدد الإشعارات المعلقة. هذه الفروق وصفية وليست معياراً طبياً أو تشخيصياً، وتبقى مقيدة بنفس قائمة آخر 30 إشعاراً المتاحة للمدير؛ لذلك لا تمثل تحليلاً تاريخياً غير محدود.

يعرض مركز الإشعارات كذلك اتجاه الاستجابة اليومي للفترة المختارة. يجمع الخادم الإشعارات المنشأة في كل يوم تقويمي بالتوقيت العالمي UTC، ويعيد لكل يوم إجمالي الإشعارات والمؤكدة وغير المؤكدة ونسبة التأكيد. تظهر الأيام التي لا تتضمن إشعارات عند صفر للحفاظ على تسلسل الرسم، ولا يشمل الاتجاه سوى الإشعارات المقيدة مسبقاً بعضويات المدير النشطة ومن قائمة آخر 30 إشعاراً؛ لذلك يعد مؤشراً تشغيلياً وصفياً وليس سجلاً تاريخياً كاملاً أو معياراً طبياً.

يتيح زر **تنزيل مؤشرات CSV** تصدير صف إجمالي واحد للفترة والعيادة المختارتين، يشمل عدد الإشعارات وحالات التأكيد والنسبة ومتوسط زمن الاستجابة. لا يتضمن الملف عناوين الرسائل أو نصوصها أو أي بيانات صحية، ويطبّق ترميز CSV وحماية الخلايا من بدايات الصيغ الحسابية نفسها المستخدمة في سجل التدقيق.

يتيح زر **تنزيل CSV للاتجاه** ملفاً منفصلاً للاتجاه اليومي في الفترة والعيادة المختارتين. يقتصر كل صف على تاريخ UTC وإجمالي الإشعارات والأعداد المؤكدة وغير المؤكدة ونسبة التأكيد؛ ولا يضم معرفات زيارات أو عناوين أو رسائل أو أي بيانات صحية. يطبّق التصدير الحماية نفسها من صيغ الجداول الحسابية، ويعيد الخادم نطاق العيادة الفعّال فقط إذا كان المدير يملك عضوية `MANAGER` نشطة فيها.

يوفر مركز الإشعارات تنبيه عتبة تشغيلياً لنسبة التأكيد ضمن فترة 7 أو 30 أو 90 يوماً. تظهر للمدير العيادات التي يملك فيها عضوية `MANAGER` نشطة فقط، ويطبق اختيار العيادة خادمياً على تقرير الاستجابة والمقارنة والاتجاه وتنبيه العتبة. يختار المدير حداً أدنى من القيم المسموحة بين 50% و90% ويحفظه الخادم بإعداد منفصل لكل زوج من الحساب والعيادة؛ ثم يعيد التطبيق استرجاعه عند اختيار العيادة. عند تغير القيمة فعلياً، يكتب الخادم حدث `NOTIFICATION_THRESHOLD_CHANGED` في سجل التدقيق مع العيادة والمنفذ والحد السابق والجديد، ولا يكرر الحدث عندما تُحفظ القيمة ذاتها. تعرض بطاقة العتبة آخر تغيير مسموح لهذه العيادة، وجدولاً موجزاً لأحدث خمسة تغييرات بملخصها ومنفذها وطابعها الزمني، أو توضح عدم وجود تغيير سابق؛ ولا تظهر أحداثاً من عيادة لا يملك المدير عضوية نشطة فيها. يتيح سجل التدقيق التشغيلي أيضاً مرشحاً صريحاً باسم **تغيير عتبة التأكيد** وشارة عربية مطابقة، كما يتيح اختيار عيادة يطبق على نتائج السجل وملف CSV معاً؛ ويرفض الخادم العيادة غير الموجودة ضمن عضويات `MANAGER` النشطة بدلاً من إرجاع أحداثها. لا يكشف السجل بيانات سريرية أو مالية. لا يظهر تجاوز للعتبة عند عدم وجود إشعارات، ولا ينشئ النظام رسالة خارج التطبيق أو تذكرة تشغيلية أو حكماً طبياً؛ إنه مؤشر وصفي يساعد في المتابعة فقط.

يوفر شريط التطبيق مسار **سجل التأكيدات** الذي يرشح `audit_events` خادمياً إلى `NOTIFICATION_ACKNOWLEDGED` فقط. تظهر كل بطاقة بتسمية عربية صريحة، وملخص الحدث، واسم المنفذ، والطابع الزمني؛ ويبقى هذا العرض مقصوراً على مدير يملك عضوية نشطة داخل العيادة.

تعرض مساحة الفريق الطبي المهام المسندة إلى الحساب الحالي فقط. وتظهر وصلة تحرير التقرير النهائي للممارس المكلّف عندما تكون الزيارة مكتملة، مع تمرير معرف الزيارة إلى المسار `/team/report/:id`. يعيد الخادم التحقق الكامل عند الحفظ، ولذلك لا يكفي الوصول إلى الرابط أو تعديل معرف الزيارة في المتصفح لتجاوز صلاحية التكليف.

تظهر الفاتورة للمريض بعد اكتمال الزيارة فقط، بينما لا يُتاح زر **السداد التجريبي** إلا إذا كانت حالة الفاتورة `DUE`. يسجل الإجراء مرجعاً اصطناعياً من صيغة `DEMO-…` ويحوّل حالة الفاتورة إلى `PAID` دون نقل أموال أو جمع أي بيانات دفع.

## نموذج البيانات المستخدم

| الجدول | مسؤوليته في التدفق |
|---|---|
| `visits` | الطلب والموعد والعيادة وحالة الزيارة ومالكها. |
| `clinic_memberships` | يربط المستخدم بالعيادة والدور التشغيلي وحالة العضوية. |
| `audit_events` | يسجل أحداث التشغيل ذات الصلة بالعيادة والفاعل والمورد والوقت بملخص محدود البيانات. |
| `visit_assignments` | يربط الزيارة بالمدير المكلف وبـ`assigneeUserId` الفعلي. |
| `visit_status_history` | يسجل انتقالات الحالة لأغراض التدقيق. |
| `medical_reports` | يحفظ التقرير النهائي الواحد ومرجع الممارس الذي أقفله. |
| `invoices` | يصدر عند اكتمال الزيارة ويحمل حالة الاستحقاق أو السداد. |
| `payments` | يسجل نتيجة الدفع التجريبي فقط مع مرجع اصطناعي. |

## الاختبارات والتحقق

يجري التحقق البرمجي بالأمر `pnpm check && pnpm test`. تضم النسخة الحالية 25 ملف اختبار و99 اختباراً ناجحاً. تغطي الاختبارات إنشاء الحجز، التفويض الإداري للتعيين، تمرير معرف عضو الفريق الفعلي، قائمة مهام العضو المقيدة بهويته، منع انتقالات الحالة غير المسموحة، ملكية التقرير والفاتورة، تسجيل السداد التجريبي، وإنشاء الممارس التجريبي، وعرض عضويات الفريق وتعديل حالتها، ووصول المدير فقط إلى قائمة التدقيق وتشغيل مرشحات العيادة ونوع الحدث والفترة الزمنية والبحث النصي المحدود والتحقق من رفض نطاق زمني مقلوب أو عبارة بحث أقصر من حرفين، إضافةً إلى رفض عيادة سجل تدقيق خارج عضوية المدير النشطة، وقبول مرشح حدث تأكيد الإشعار وتمرير مرشحات CSV للمدير ورفض التصدير لغير المدير، وسياسة الزيارة المتأخرة، وتأكيد إشعار واحد أو كل الإشعارات من قبل المدير فقط. كما تمت مراجعة لوحة المدير ومساحة الفريق ومحرر التقرير على شاشات مكتبية ومحمولة بعد معالجة تجاوز العرض في RTL.

## الحدود والضوابط المتبقية

تطبّق هذه النسخة مصادقة خادمية ونطاقاً للعيادة وقيود ملكية وإجراءات تفويض، لكنها **لا تطبق Row-Level Security فعلية** لأن قاعدة البيانات الحالية MySQL/TiDB؛ لذا لا ينبغي وصفها كتنفيذ RLS إنتاجي. كذلك لا تحتوي على تكامل دفع حقيقي أو تشفير مخصص للمحتوى السريري أو إدارة وصول مؤسسية كاملة أو سجلات امتثال قانوني.

قبل استخدام النظام خارج العرض الأكاديمي، يلزم استبدال العضو التجريبي بإدارة حسابات حقيقية ومراجعة نموذج الصلاحيات، وتطبيق تشفير مناسب وإدارة مفاتيح وسجل تدقيق غير قابل للعبث وسياسات الاحتفاظ بالبيانات، فضلاً عن مراجعة قانونية وأمنية مستقلة.
