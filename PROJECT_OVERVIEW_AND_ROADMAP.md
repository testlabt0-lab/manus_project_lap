# MediCare Pro - نظرة عامة شاملة وخارطة الطريق والتطوير المستقبلية

## 1. ما الذي يعمله هذا المشروع حالياً؟ (Project Overview)

مشروع **MediCare Pro** هو منصة رقمية متكاملة لإدارة الخدمات والزيارات الطبية المنزلية (Home Health Visit Management System). تم تصميمه بالكامل ليدعم اللغة العربية وواجهات الواجهة من اليمين إلى اليسار (RTL) مع تجربة مستخدم حديثة ومستجيبة.

### أبرز الميزات المكتملة وحالة المشروع الحالية:

* **إدارة الزيارات الطبية (Visit Lifecycle):**
  * دورة حياة كاملة للزيارة (دورة حالة محددة: `pending` -> `scheduled` -> `in_progress` -> `completed` / `cancelled`).
  * تتبع حالة الزيارة مباشرة (Real-time tracking) وتحديد الأطباء/الممرضين المكلّفين بالزيارة.
  * سجل الملاحظات والتقارير الطبية لكل زيارة (Clinical Notes / Medical Reports).

* **إدارة المرضى والأطباء (Patients & Clinicians Directory):**
  * سجلات شاملة للمرضى (البيانات الشخصية، العنوان، السجل الطبي، والتاريخ الصحي).
  * دليل الأطباء والممارسين الصحيين وتحديد التخصصات والجاهزية.

* **الفواتير والمحاسبة (Billing & Invoices):**
  * إصدار وتتبّع فواتير الزيارات وحالة الدفع (مدفوع، معلق، ملغى).
  * حساب تكاليف الخدمات الطبية والزيارات.

* **سجل الأنشطة والأمان (Audit Logs):**
  * تسجيل تفصيلي لكافة العمليات الحساسة في النظام (إنشاء زيارة، تعديل تقرير، تغيير حالة، إلخ).

* **مركز الإشعارات (Notification Center):**
  * نظام تنبيهات داخل المنصة (In-app notifications) للتنبيه بالزيارات الجديدة أو التحديثات.

---

## 2. البنية التقنية للمشروع (Tech Stack & Architecture)

* **الواجهة الأمامية (Frontend):**
  * **React 19** مع **Vite** للتحميل السريع والأداء الفائق.
  * **Tailwind CSS v4** مع دعم كامل للـ RTL والتصميم المتجاوب (Responsive Design).
  * **Lucide React** للأيقونات الطبية واللوجستية.
  * **Wouter / React Router** للتنقل بين الصفحات.

* **الواجهة الخلفية والربط (Backend & API):**
  * **Node.js + Express** لخادم التطبيق.
  * **tRPC v11** للربط المباشر المضمون بالأنواع (End-to-end Type Safety) بين الواجهة الأمامية والخلفية دون الحاجة لكتابة REST schemas يدويًا.

* **قاعدة البيانات (Database & ORM):**
  * **Drizzle ORM** لإدارة قاعدة البيانات والاستعلامات فائقة الأداء مع TypeScript types تلقائية.
  * **PostgreSQL / MySQL / SQLite** دعم قواعد البيانات العلاقية السريعة.

---

## 3. كيفية تطوير المشروع ومقترحات التحسين (Development Strategy & Roadmap)

بناءً على تحليل الكود والاحتياجات المحددة في **خطة التطوير الأربعة (Plan Steps)**، نوصي بتنفيذ الخارطة الاستراتيجية التالية:

---

### Phase 1: إشعارات تلغرام الفورية للآدمن (Telegram Admin Alerts)
* **الهدف:** إرسال إشعارات فورية إلى حساب/قناة تلغرام الخاصة بالإدارة عند حدوث أحداث حرجة في النظام.
* **الأحداث المشمولة:**
  1. طلب زيارة طبية جديدة (New Visit Booking).
  2. إلغاء زيارة أو تغيير طارئ في الجدول الزمن (Visit Cancellation/Reschedule).
  3. تنبيهات الأمان والدخول المشبوه أو تفعيل أجهزة جديدة.
* **آلية التنفيذ:**
  * إنشاء Bot على تلغرام عبر `@BotFather`.
  * إضافة خدمة `TelegramService` في المجلد `server/services/telegram.ts`.
  * ربط الأحداث في tRPC mutations بإرسال رسائل نسقّة عبر Telegram Bot API.

---

### Phase 2: تعزيز الأمان وتشفير البيانات والربط بالأجهزة (Security, Encryption & Device Binding)

1. **تشفير البيانات الحساسة (Sensitive Data Encryption):**
   * تشفير سجلات المرضى والتقارير الطبية الحساسة أثناء التخزين (Data-at-Rest Encryption) باستخدام مشفرات هندسية مثل `AES-256-GCM`.
   * إخفاء البيانات الشخصية (Data Masking) في سجلات التدقيق (Audit Logs).

2. **النسخ الاحتياطي التلقائي (Automated Backups):**
   * ضبط سكريبتات نسخ احتياطي دورية لقاعدة البيانات (Cron Jobs) مع التشفير والحفظ في مستودع سحابي آمن (Cloud Storage S3 / Offsite).

3. **ربط التطبيق بالجهاز وتفعيله (Device Binding & Activation):**
   * إنشاء آلية لربط حساب المستخدم (طبيب / آدمن) بجهاز محدد عبر معرف الجهاز الفريد (Device UUID / Fingerprint).
   * إمكانية تفعيل/تعطيل الأجهزة المعتمدة من قبل الآدمن لمنع تسجيل الدخول من أجهزة غير مصرح بها.

4. **حماية التطبيق من الهندسة العكسية (Anti-Reverse Engineering & Obfuscation):**
   * تطبيق تلميع وحماية الأكواد (JavaScript / Native Obfuscation).
   * تطبيق تشفير الاتصالات (SSL Pinning) وحماية الرموز البرمجية (Code Integrity Checks).

---

### Phase 3: إدارة الصلاحيات المتقدمة (Advanced RBAC System)

* **الهدف:** حوكمة دقيقة للوصول إلى البيانات حسب الأدوار والتخصصات.
* **الأدوار الرئيسية:**
  * **Super Admin (مدير النظام الشامل):** صلاحية كاملة للنظام والأجهزة والإعدادات المالية.
  * **Dispatcher / Operations Admin (مشرف العمليات):** إدارة وتوزيع الزيارات على الأطباء.
  * **Clinician / Doctor (الطبيب/الممرض):** الاطلاع على زياراته الخاصة وكتابة التقارير الطبية فقط.
  * **Patient / Customer (المريض):** طلب زيارة وتتبع حالتها واستعراض الفواتير.
* **التطبيق التقني:**
  * بناء Middleware في tRPC يسمى `protectedProcedure.hasPermission('PERMISSION_NAME')`.
  * مصفوفة مخصصة للدوال والمسارات المسموحة لكل دور.

---

### Phase 4: استراتيجية تطوير تطبيق الموبايل (Android App Development Strategy)

توجد استراتيجيتان رئيسيتان لتطوير تطبيق أندرويد لهذا المشروع:

#### الخيار الأول: استخدام React Native / Expo (الخيار الموصى به)
* **المميزات:**
  * إعادة استخدام أكثر من **70% من الكود الحالي** (Logic, tRPC types, validation, UI components).
  * تطوير سريع جداً وتحديثات هوائية (OTA updates).
  * أداء عالٍ قريب جداً من النيتف وتوافق تام مع نظام Android و iOS.

#### الخيار الثاني: استخدام Capacitor / PWA Wrapper (التطبيق الهجين المباشر)
* **المميزات:**
  * تغليف تطبيق الويب الحالي وتثبيته كـ APK على أجهزة الأندرويد خلال ساعات قليلة.
  * استخدام الكود الحالي بنسبة 100%.

#### الخيار الثالث: Android Native (Kotlin)
* **المميزات:** أداء مطلق للتعامل مع العتاد في حال وجود أجهزة طبية مرئية ترتبط عبر Bluetooth مباشر، لكن يتطلب بناء الواجهات والمنطق من الصفر.

---

## 4. ملخص خطوات العمل البرمجية القادمة (Actionable Checklist)

- [ ] إكمال بناء وتوثيق موديولات الخدمة الحالية.
- [ ] إضافة `TelegramBotService` للربط مع الآدمن.
- [ ] إضافة طبقة تشفير البيانات الحساسة (`encryption.ts`) في قواعد البيانات.
- [ ] إنشاء جدولة النسخ الاحتياطي التلقائي.
- [ ] إضافة شاشة إدارة الأجهزة المرتبطة وتحديد الأجهزة المعتمدة في لوحة التحكم.
- [ ] بدء مرحلة تجهيز تطبيق الأندرويد (React Native / Capacitor setup).
